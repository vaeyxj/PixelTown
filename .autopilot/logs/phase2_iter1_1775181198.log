Phase 2 迭代 1 完成。

**本次做了什么：**
- 将 `parallax.ts` 中的背景层集成进 `engine.ts` 渲染循环：
  - `bgLayer` 添加到 `app.stage`（在 `worldContainer` 之前，确保渲染在底层）
  - 每帧 `camera.update()` 之后调用 `applyParallax(scrollFactor=0.3)`，背景以 30% 速度滚动
  - `destroy()` 时正确清理 `bgLayer`
  - engine.ts 保持 199 行（< 200 行要求）

**质量门：** BUILD ✓ / TEST ✓ (11 tests)

**验收标准核查：**
- [x] 视差效果可见（背景层 0.3x 滚动速度）
- [x] engine.ts < 200 行（199 行）
- [x] `pnpm build` exit 0
- [x] `pnpm test` 通过
